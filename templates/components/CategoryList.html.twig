<div class="card card-body mb-4">
    <h3 class="h5">Categories</h3>
    <ul class="nav flex-column fs-sm">
        {% for category in this.categories %}
        <li class="nav-item mb-1">
            <a href="#" class="nav-link py-1 px-0">{{ category.name }}</a>
        </li>
        {% endfor %}
    </ul>
</div>
