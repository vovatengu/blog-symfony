{% extends "base.html.twig" %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <h1>Edit Post</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.body) }}
        <button class="submit">Update</button>
    {{ form_end(form) }}
{% endblock %}
